/*
 * fsm_auto.c
 *
 *  Created on: Nov 4, 2022
 *      Author: Admin
 */

#include "fsm_auto.h"

void fsm_simple_buttons_run (){
	/*switch(LED_BLINK){
	case INIT:
		LED_BLINK = BLINK;
		setTimer1(100);
		break;
	case BLINK:
		if(timer1_flag == 1){
			RIDE_ON();
			setTimer1(100);
		}
		break;
	default:
		break;
	}*/
	for(int i = 0; i < 10; i++) display7SEG1(i);
}
